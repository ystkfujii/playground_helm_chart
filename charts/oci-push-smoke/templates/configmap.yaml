apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "oci-push-smoke.fullname" . }}
  labels:
    {{- include "oci-push-smoke.labels" . | nindent 4 }}
data:
  message: {{ .Values.message | quote }}
